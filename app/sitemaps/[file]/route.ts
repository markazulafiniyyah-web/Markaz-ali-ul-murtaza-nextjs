import { LANG_ORDER } from "@/lib/i18n";
import { requestOrigin } from "@/lib/request-origin";
export const dynamic="force-dynamic";
function render(urls:string[]){return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(url=>`<url><loc>${url}</loc></url>`).join("")}</urlset>`}
export async function GET(request:Request,{params}:{params:Promise<{file:string}>}){const base=requestOrigin(request);const {file}=await params;if(file!=="pages.xml")return new Response("Not found",{status:404});const localized=LANG_ORDER.flatMap(lang=>[`${base}/${lang}`,`${base}/${lang}/donate`,`${base}/${lang}/portfolio`]);const urls=[...localized,`${base}/hifz`,`${base}/nazra`,`${base}/tajweed`,`${base}/online-quran-classes`,`${base}/online-hifz-tajweed-institute`,`${base}/jamia-riaz-ul-jannah`,`${base}/terms`,`${base}/blog`,`${base}/blog/from-recitation-to-a-life-guided-by-quran`];return new Response(render(urls),{headers:{"content-type":"application/xml; charset=utf-8","cache-control":"public, s-maxage=3600, stale-while-revalidate=86400","vary":"host, x-forwarded-host, x-forwarded-proto"}})}
