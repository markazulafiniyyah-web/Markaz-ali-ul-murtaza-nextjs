# Markaz Ali ul Murtaza — Next.js

A native Next.js 16 App Router application for Markaz Ali ul Murtaza. The project contains no injected legacy pages or static-site source.

## Structure

```text
app/                    Next.js routes, metadata, API, and global styles
  api/hadith/[slug]/    Cached Hadith JSON API
  donate/               Donation page
  hadith/               Interactive Hadith library
components/             Native React UI and feature components
data/hadith/            Canonical data for all 17 Hadith collections
lib/                     Typed i18n and Hadith registries
public/images/           Image and SVG assets
```

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Production

```bash
npm run build
npm start
```

## Routes

- `/` — institute homepage
- `/donate` — interactive donation page
- `/hadith` — searchable Hadith library
- `/api/hadith/[slug]` — immutable, cached Hadith collection data
- `/robots.txt` and `/sitemap.xml` — generated by Next.js

## Features

- Native React components and hooks
- 30-language interface with RTL support
- 17 Hadith books and 50,884 records
- Search, chapter filters, pagination, bookmarks, display modes, and text sizing
- Machine-translation support with local caching
- Donation calculator, clipboard actions, and WhatsApp handoff
- Responsive navigation and accessible page structure
